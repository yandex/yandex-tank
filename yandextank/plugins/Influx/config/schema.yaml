tank_tag:
  default: unknown
  type: string
address:
  default: localhost
  type: string
port:
  default: 8086
  type: integer
database:
  default: mydb
  type: string
username:
  default: root
  type: string
password:
  default: root
  type: string
grafana_root:
  default: "http://localhost/"
  type: string
grafana_dashboard:
  default: tank-dashboard
  type: string
chunk_size:
  default: 500000
  type: integer