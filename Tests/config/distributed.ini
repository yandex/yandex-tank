[distributed]
api_port=8003
api_timeout=5

tanks_pool=tank1 tank2
  tank3
  tank4

tanks_count=2 ; all means all, 1 by default

random_tanks=1

configs=load.conf jmeter.conf
  autostop.conf

options=phantom.autocases=1 monitoring.ssh_timeout=5 "phantom.rps_schedule=line(1,100, 5m)"

files=mon.xml mon1.xml
  data/dummy.ammo

download_artifacts=*.log *.ini

